name: eventeasee
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0' # Safe unified sdk distribution bounds

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^17.0.0
  fl_chart: ^0.66.0
  # --- CLEAN ALIGNED DEPENDENCIES SECTION ---
  screenshot: ^3.0.0
  qr_flutter: ^4.1.0
  cupertino_icons: ^1.0.8
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  provider: ^6.1.5+1
  google_fonts: ^8.0.2
  mobile_scanner: ^7.2.0
  image_picker: ^1.2.2
  firebase_storage: ^13.4.2
  http: ^1.6.0
  csv: ^8.0.0
  share_plus: ^13.1.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1 # <-- 1. Dev dependency add ki

# <-- 2. Launcher icon ki configuration set ki
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/app_icon.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true